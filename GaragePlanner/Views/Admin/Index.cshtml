<!DOCTYPE html>
<html>
<head>
    <title>Admin Page</title>
</head>
<body>
<h1>Car Brands Admin</h1>
    
<h2>Existing Brands</h2>
<ul>
    @foreach (var brand in Model.Brands)
    {
        <li>@brand</li>
    }
</ul>
    
<h2>Add Brand</h2>
    <form method="post" action="@Url.Action("AddBrand", "Admin")">
    <label for="brandName">Brand Name:</label>
    <input type="text" id="brandName" name="brandName" required>
    <input type="submit" value="Add Brand">
</form>
    
<h2>Delete Brand</h2>
    <form method="post" action="@Url.Action("DeleteBrand", "Admin")">
    <label for="deleteBrand">Select Brand:</label>
    <select id="deleteBrand" name="deleteBrand" required>
        @foreach (var brand in Model.Brands)
        {
            <option value="@brand">@brand</option>
        }
    </select>
    <input type="submit" value="Delete Brand">
</form>
</body>
</html>